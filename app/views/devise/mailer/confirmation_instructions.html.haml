%p
  = t("devise.sessions.hello", user: @resource.email)

%p
  = t("devise.confirmations.confirmation_instructions")
%p
  = link_to t("devise.confirmations.confirm_my_account"), "#{AppConfig.url}#{confirmation_path(@resource, :confirmation_token => @resource.confirmation_token)}"
